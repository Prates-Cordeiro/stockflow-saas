{
  "name": "stockflow-saas-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "description": "Root tooling for the StockFlow SaaS monorepo: husky + commitlint. The Next.js app lives under apps/ once scaffolded.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  },
  "engines": {
    "node": ">=22"
  }
}
